import { numberWizard } from '../../../helpers/number-wizard';
import { module, test } from 'qunit';

module('Unit | Helper | number wizard');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = numberWizard(42);
  assert.ok(result);
});
