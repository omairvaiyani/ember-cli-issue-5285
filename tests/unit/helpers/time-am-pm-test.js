import {
  timeAmPm
} from 'mycqs-web/helpers/time-am-pm';

module('TimeAmPmHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = timeAmPm(42);
  ok(result);
});
