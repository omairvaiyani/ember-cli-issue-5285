import {
  highlightString
} from 'mycqs-web/helpers/highlight-string';

module('HighlightStringHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = highlightString(42);
  ok(result);
});
