import {
  stripePrice
} from 'mycqs-web/helpers/stripe-price';

module('StripePriceHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = stripePrice(42);
  ok(result);
});
