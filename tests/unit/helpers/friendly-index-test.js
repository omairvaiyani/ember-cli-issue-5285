import {
  friendlyIndex
} from '../../../helpers/friendly-index';
import { module, test } from 'qunit';

module('FriendlyIndexHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = friendlyIndex(42);
  assert.ok(result);
});
