import {
  simpleDate
} from 'mycqs-web/helpers/simple-date';

module('SimpleDateHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = simpleDate(42);
  ok(result);
});
