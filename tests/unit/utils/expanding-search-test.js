import expandingSearch from 'mycqs-web/utils/expanding-search';

module('expandingSearch');

// Replace this with your real tests.
test('it works', function() {
  var result = expandingSearch();
  ok(result);
});
