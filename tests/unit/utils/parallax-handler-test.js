import parallaxHandler from 'mycqs-web/utils/parallax-handler';

module('parallaxHandler');

// Replace this with your real tests.
test('it works', function() {
  var result = parallaxHandler();
  ok(result);
});
