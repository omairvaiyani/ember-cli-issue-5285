import cryptoHash from '../../../utils/crypto-hash';
import { module, test } from 'qunit';

module('Unit | Utility | crypto hash');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = cryptoHash();
  assert.ok(result);
});
