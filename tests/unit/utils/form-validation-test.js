import formValidation from 'mycqs-web/utils/form-validation';

module('formValidation');

// Replace this with your real tests.
test('it works', function() {
  var result = formValidation();
  ok(result);
});
