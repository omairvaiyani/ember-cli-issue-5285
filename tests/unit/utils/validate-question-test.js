import validateQuestion from 'mycqs-web/utils/validate-question';

module('validateQuestion');

// Replace this with your real tests.
test('it works', function() {
  var result = validateQuestion();
  ok(result);
});
