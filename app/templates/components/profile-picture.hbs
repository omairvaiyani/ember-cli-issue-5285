{{#unless doNotLink}}
    {{#link-to 'user.index' user.slug}}
        {{view 'profile-image' user=user height=height width=width}}
    {{/link-to}}
{{else}}
    {{view 'profile-image' user=user height=height width=width}}
{{/unless}}

