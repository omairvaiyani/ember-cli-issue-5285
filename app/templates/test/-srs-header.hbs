{{#if isSRSAttempt}}
    Spaced Repetition Test
    <small>Intensity level: {{currentUser.spacedRepetitionIntensity}}</small>
{{else}}
    {{#if isProfessional}}
        Generated {{questionBank}} test
    {{/if}}
{{/if}}