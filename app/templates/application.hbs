{{#sliding-menu currentUser=currentUser parentController=this}}
    {{partial 'navigation-bar'}}

    {{outlet}}
{{/sliding-menu}}


{{partial 'footer'}}

{{view 'notification-container'}}

{{#modal-dialog}}
    {{outlet 'modal'}}
{{/modal-dialog}}